{
  "name": "diary-analyzer-web-app",
  "version": "1.0.0",
  "description": "A beautiful web application for visualizing Google Calendar diary entries in a clean timeline format",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 8000",
    "dev": "python -m http.server 8000",
    "serve": "npx http-server -p 8000 -c-1",
    "build": "echo 'No build step required for static web app'",
    "test": "echo 'No tests specified' && exit 0",
    "lint": "echo 'No linting configured' && exit 0",
    "clean": "echo 'No cleanup required for static web app'"
  },
  "keywords": [
    "web-app",
    "google-calendar",
    "timeline",
    "diary",
    "visualization",
    "productivity",
    "oauth"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "sharp": "^0.32.0"
  },
  "repository": {
    "type": "git",
    "url": "your-repository-url"
  },
  "bugs": {
    "url": "your-repository-url/issues"
  },
  "homepage": "your-repository-url#readme"
}